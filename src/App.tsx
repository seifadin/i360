import { AppStateProvider } from '@/store/appState'

function App() {
  return (
    <AppStateProvider>
      <div>
        <h1>i360إ</h1>
      </div>
    </AppStateProvider>
  )
}

export default App
